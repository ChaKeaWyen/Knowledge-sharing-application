import 'package:flutter/material.dart';

class UserFavPage extends StatelessWidget {
  const UserFavPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('FAVOURITE'),
      ),
    );
  }
}
